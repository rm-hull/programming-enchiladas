;; This file allows CLJS code to use (:require-macros [enchilada.util.macros])
;; The actual macro definitions are in the .clj file.
(ns enchilada.util.macros
  (:require-macros [enchilada.util.macros]))
